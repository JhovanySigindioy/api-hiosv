{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",                              /* Establece la versión de JavaScript para el código emitido. */
    "module": "commonjs",                             /* Especifica el tipo de módulos que se generan. */
    "rootDir": "./src",                               /* Especifica la carpeta raíz dentro de tus archivos fuente. */
    "outDir": "./dist",                               /* Especifica la carpeta de salida para todos los archivos emitidos. */
    "esModuleInterop": true,                          /* Permite la interoperabilidad entre módulos ES y CommonJS. */
    "forceConsistentCasingInFileNames": true,         /* Asegura que el nombre de archivo sea consistente (importaciones sensibles a mayúsculas/minúsculas). */
    "strict": true,                                   /* Habilita todas las opciones estrictas de comprobación de tipos. */
    "skipLibCheck": true                              /* Omite la comprobación de tipos en archivos .d.ts. */
  },
  "include": [
    "src/**/*.ts"                                        /* Asegúrate de que todos los archivos .ts de la carpeta src se incluyan. */
  ],
  "exclude": [
    "node_modules"                                    /* Excluye la carpeta node_modules. */
  ]
}
